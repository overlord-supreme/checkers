using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Handles:
/// - Menu UI (Buttons)
/// - Match Fetching
/// - Match Display Updating
/// - Match Join Requests
/// </summary>
public class LobbyMenu : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
